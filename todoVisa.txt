Sprint 1 - Demande de visa transformable

Contexte et objectif
- Livrer uniquement la fonctionnalite "Nouveau titre" pour visa transformable.
- Profils cibles : investisseur et travailleur

Organisation des branches
- Jemima : sprint/1/feature/formDemande-back
- Mioty : sprint/1/feature/formDemande-front

Fonctionnalite du Sprint 1 :
- Section "Type de demande" : "Nouveau titre" est le seul actif.
- Le formulaire change selon le choix : investisseur ou travailleur.
- Le numero de passeport reste identique.
- Le statut "demande creee" est attribue uniquement si tous les obligatoires sont valides.

Jemima - Backend 
1. Models a preparer
- [X] Demande 
- [X] Demandeur
- [X] Passeport
- [X] VisaDTO
- [X] PieceJustificative

2. Controller
- DemandeController.java

3. Routes
- Acces formulaire
    - @MethodeAnnotation("/visa/form")
    - @GetMapping
- Soumission formulaire
    - @MethodeAnnotation("/visa/form")
    - @PostMapping
- Retour erreur validation
    - @MethodeAnnotation("/visa/form/error")
    - @GetMapping
- Enregistrement demande
    - @MethodeAnnotation("/visa/form/save")
    - @PostMapping
- Mise a jour demande existante
    - @MethodeAnnotation("/visa/form/update")
    - @PostMapping

4. Fonctions metier a implementer
- Validation des champs obligatoires : isObligatoire()
- Creation demande : saveDemande() -> statut "demande creee"
- Differenciation investisseur/travailleur : getInfoSpecifique(idType)
- Modification demande existante : updateDemande()
- Recuperation des pieces selon le type de titre :
    - type_titre IS NULL -> pieces communes
    - type_titre = investisseur/travailleur -> pieces specifiques

5. Regles de validation metier
- Si obligatoires valides : sauvegarder et mettre statut "demande creee".
- Sinon : rediriger vers l'ecran d'erreur avec details de validation.


Mioty - Frontend 
1. Vue principale
- Creer nouvelleDemande.jsp.
- Ajouter un select "Type de demande" avec :
    - Nouveau titre 
    - Duplicata 
    - Transfert de visa 

2. Affichage conditionnel des champs
- Champs obligatoires 
    - Etat civil : nom, nom de jeune fille, date de naissance, situation de famille (id), nationalite (id), adresse Madagascar, numero telephone, profession
    - Passeport : numero, date de delivrance, date d'expiration, pays de delivrance
    - Visa : date d'entree, lieu d'entree, date d'expiration, categorie de demande
    - les piece justificatives a coches
- Champs optionnels
    - prenom
    - email

3. Pieces justificatives (cases a cocher)
- En commun
    - 02 photos d'identite
    - Notice de renseignement
    - Demande adressee a Mr le Ministre de l'Interieur et de la Decentralisation (avec e-mail et numero portable)
    - Photocopie certifiee du visa en cours de validite
    - Photocopie certifiee de la premiere page du passeport
    - Photocopie certifiee de la carte de resident en cours de validite
    - Certificat de residence a Madagascar
    - Extrait de casier judiciaire de moins de 3 mois

- Si investisseur, on ajoute
    - Statut de la societe
    - Extrait d'inscription au registre du commerce
    - Carte fiscale
- Si travailleur, on ajoute
    - Autorisation d'emploi delivree a Madagascar par le Ministere de la Fonction publique
    - Attestation d'emploi delivree par l'employeur (original)

- Activer "Soumettre" seulement si les obligatoires sont completes.
- Afficher clairement les erreurs/champs incomplets avant envoi.
- Permettre la modification d'une demande deja creee en rechargeant les donnees dans le formulaire.

--------------------------------------------------------------------------------------------------------------------------
Sprint 2 - duplicata titre residence (pas d'antecedent en base)

NB : Lancer le script alter_sprint2.sql apres table.sql pour ajouter les nouvelles tables et colonnes necessaires

Contexte

Un demandeur arrive au guichet, il dit qu'il a perdu son titre de residence.
Sauf que dans la base on retrouve rien (ancienne demande introuvable).

Objectif

* on ouvre un nouveau dossier
* on met une mention "duplicata — antecedent non retrouvé"
* l'agent confirme a la main que le visa etait deja approuvé
* et on lance le duplicata normal

1. Database - Jemima (TL) Branche: dev

* Ajout des table document et type_document
* Changement dans la table demande :

  * type_document_id
  * ref_demande
* Ajout des tables dossier et dossier_demande :

  * dossier : id, previous_demande_ref, new_demande_ref, mention, visa_approuve_confirme, created_at, updated_at
  * dossier_demande : id, dossier_id, demande_id (suivi des dossier rajouter a la demande)
* Ajout statuts manquants dans statut_demande :

  * En cours de traitement, En attente, Valide, Refuse
* script de migration : alter_sprint2.sql

  * a ajouter dans start.bat apres table.sql

2. Back-end - Mioty : Branche: sprint/2/feature/recherche-dossier

Module Recherche :

Pour voir si la demande est presente ou pas dans la base de donnée

* Classe: DemandeDAO
  => prototype: findBy(colonne, valeur)
* Classe: DemandeRepository
  => fonction: findBy(colonne, valeur) => retourne une demande ou null
* Classe: DemandeService
  => fonction: searchBy(colonne, valeur) => appel repository.findBy(colonne, valeur)

chercher demandeur avec :

* nom
* prenom
* date naissance

Pour voir si le passeport est presente ou pas dans la base de donnée

* Classe: PasseportDAO
  => prototype: findByNum(numero_passeport)
* Classe: PasseportRepository
  => fonction: findByNum(colonne, valeur) => retourne un passeport ou null
* Classe: PasseportService
  => fonction: getByNum(colonne, valeur) => appel repository.findByNum(colonne, valeur)

chercher passeport avec :

* numero_passeport

Si trouvé

* on reutilise demandeur + passeport
* flow normal

Si pas trouvé

* on cree :

  * nouveau demandeur
  * nouveau passeport
* Statut de la demande de visa : Valide
* Statut de la demande de titre de residence / duplicata ou autre : En cours de traitement

Module Dossier :

Pour creer et suivre un dossier

* Classe: DossierDAO
  => prototype: create(dossier)
  => prototype: findByDemande(demande_id)

* Classe: DossierRepository
  => fonction: create(dossier) => retourne le dossier cree
  => fonction: findByDemande(demande_id) => retourne un dossier ou null

* Classe: DossierService
  => fonction: ouvrirDossierDuplicata(demandeur, passeport, previous_demande_ref)
  - cree la demande avec type_demande = Duplicata, type_document = Titre de residence
  - statut = En cours de traitement
  - genere ref_demande format : YYYYMMDD-HHMMSS-DUP
  - cree le dossier avec :
  + mention = "Duplicata — antecedent non retrouve"
  + visa_approuve_confirme = true
  + previous_demande_ref = ref fournie par le demandeur (nullable)
  + new_demande_ref = ref_demande generee
  - insere dans dossier_demande

  => fonction: attacherDemande(dossier_id, demande_id)
  - insere dans dossier_demande si pas deja present

Regles metier a enforcer cote application :

* type_document_id obligatoire si type_demande = Duplicata
* previous_demande_ref renseigne => new_demande_ref obligatoire
* un passeport ne peut pas etre rattache a deux demandeurs differents

3. Front-end - Elia - Branche : sprint/2/feature/refact-flow-demande

Recherche

Avant le formulaire :

* champ recherche :
  nom / prenom / date naissance

si trouvé :

* afficher infos demandeur + passeport
* actions :

  * Mettre a jour
  * Nouvelle demande (choix type)

si pas trouvé :

* message : "Aucun demandeur trouvé..."
* choix type de demande
* si visa => flux normal
* sinon => creation dossier avec statut visa valide

Etape 2 : formulaire

Bloc A (normal)

* infos demandeur
* infos passeport

Bloc B (duplicata)

* checkbox : visa approuve
* champ : previous_demande_ref (optionnel)
* pieces justificatives (au moins une obligatoire)
* mention (read only) :
  "Duplicata — antecedent non retrouvé"

Etape 3 : soumission

* validation :

  * Bloc A complet
  * checkbox cochée
  * au moins une piece
  * type_document = Titre de residence
* appel DossierService.ouvrirDossierDuplicata()
* redirect vers suivi dossier

Suivi dossier

Afficher :

* mention (badge orange)
* statut
* visa confirme

Donnees :

* ref_demande
* type_demande + type_document
* nom complet
* statut (couleur)

  * En cours => bleu
  * En attente => orange
  * Valide => vert
  * Refuse => rouge
* pieces justificatives
* mention dossier
* "Visa approuvé confirmé par agent"
-----------------------------------------------------------------------------------------------------------
Sprint 3 - Finalisation et Scan (SCAN TERMINE)

Contexte

L’operateur a saisi la demande (Sprint 1) et a eventuellement gere un duplicata (Sprint 2).
Il possede maintenant les documents physiques du demandeur.
Il doit les numeriser et les rattacher a chaque piece justificative attendue.

Une fois toutes les pieces scannees, l’operateur verrouille le dossier : plus aucune modification possible sur la demande, le demandeur ou le passeport.
Le statut de la demande passe alors de CREE ou "demande creee" a SCAN TERMINE.

Objectif

* Ajouter la gestion des fichiers scannes (upload) par piece justificative
* Controler la completude : bouton "SCAN TERMINE" actif uniquement quand toutes les pieces attendues (cochees dans demande_piece) ont un fichier associe
* Verrouillage definitif des donnees apres clic sur "SCAN TERMINE"
* Nouveau statut SCAN TERMINE dans la table statut_demande

---

1. Database - Mioty (TL)
   Branche : sprint/3/feature/scan-finalisation

Nouvelles tables et colonnes

* Ajouter table piece_fournie pour stocker les fichiers scannes
* Ajout du statut SCAN TERMINE dans statut_demande
* Ajout d’une colonne verrouille BOOLEAN DEFAULT FALSE dans la table demande

Index :

* idx_piece_fournie_demande sur piece_fournie(demande_id)
* idx_piece_fournie_piece sur piece_fournie(piece_ref_id)

Script de migration : alter_sprint3.sql
A executer apres alter_sprint2.sql dans start.bat

---

2. Back-end - Elia
   Branche : sprint/3/feature/scan-backend

Nouvelles classes

DAO

* PieceFournieDAO

  * create(PieceFournie p)
  * findByDemandeAndPieceRef(demandeId, pieceRefId)
  * findAllByDemande(demandeId)
  * deleteByDemandeAndPieceRef(demandeId, pieceRefId)

Repository

* PieceFournieRepository

  * sauvegarder(piece)
  * recuperer(demandeId, pieceRefId)
  * listerParDemande(demandeId)
  * supprimer(demandeId, pieceRefId)

Service

* ScanService

  * uploadPiece(demandeId, pieceRefId, Part fichier)
    -> sauvegarde fichier sur disque + en base
    -> remplace l’existant
    -> verifie taille max 10 Mo
    -> verifie types autorises (jpeg, png, pdf)
    -> lance DemandeVerrouilleeException si verrouille

  * isDemandeComplete(demandeId)
    -> true si chaque piece cochee a une piece_fournie

  * verrouillerDemande(demandeId)
    -> verifie completude
    -> met verrouille = true
    -> statut = SCAN TERMINE

* DemandeService (modification)

  * updateDemande, updateDemandeur, updatePasseport
    -> verifier verrouille == false
    -> sinon DemandeVerrouilleeException

Controllers MVC

* ScanController

  GET /demande/{id}/scan

  * affiche scanDemande.jsp
  * model : demande, listePiecesAttendues, demandeComplete
  * si verrouille => bloquer interface

  POST /demande/{demandeId}/piece/{pieceRefId}/upload

  * upload fichier
  * redirection avec message

  POST /demande/{demandeId}/verrouiller

  * appelle verrouillerDemande
  * redirection vers /suivi?ref=...

  GET /demande/{demandeId}/piece/{pieceRefId}/download

  * telechargement fichier

Controleur complementaire

* SuiviController

  * GET /suivi?ref=...
  * ajouter piece_fournie au modele

---

Regles metier

* Taille max : 10 Mo
* Types autorises :
  image/jpeg
  image/png
  application/pdf
* Une piece peut etre remplacee
* Verrouillage irreversible
* Si aucune piece attendue => complet = true

---

3. Front-end - Jemima
   Branche : sprint/3/feature/scan-front

Page : scanDemande.jsp

URL : /demande/{id}/scan

Bloc en-tete

* ref_demande
* nom complet
* statut (badge)
* si verrouille => message rouge

Bloc pieces

Pour chaque piece cochee :

* libelle

* etat :

  * fichier present => nom, taille, date, download
  * sinon => aucun fichier

* formulaire upload :

  * input file (jpeg, png, pdf)
  * bouton upload

Bloc bouton SCAN TERMINE

* POST /demande/{id}/verrouiller
* bouton desactive si incomplet
* confirmation JS :
  confirm("Toutes les pieces sont scannees. Verrouiller ?")

---

Suivi dossier (suiviDossier.jsp)

Section : Documents scannes

* liste piece_fournie
* nom + lien download
* si vide => aucun document

Statut

* afficher SCAN TERMINE avec badge violet

---

4. Routes

ScanController

* GET /demande/{id}/scan
* POST /demande/{demandeId}/piece/{pieceRefId}/upload
* POST /demande/{demandeId}/verrouiller
* GET /demande/{demandeId}/piece/{pieceRefId}/download

SuiviController

* GET /suivi
  -> ajoute piecesScannees

DemandeController

* toutes les modifications doivent verifier verrouille
* sinon erreur ou redirection
---------------------------------------------------------------------------------------------
Sprint 4 - Scan Qr code
Contexte: 
On veut une application frontend (Vue.js) qui permet de suivre les demandes d’un utilisateur en entrant soit un numéro de demande, soit un numéro de passeport. L’application appelle une API REST développée en Spring Boot.
Si l’utilisateur entre un numéro de passeport, toutes ses demandes s’affichent avec leurs statuts, triées par ordre chronologique (la plus récente en premier). Si c’est un numéro de demande, cette demande apparaît en premier, suivie des autres demandes liées.
Chaque demande doit afficher un historique de statuts (ex : dossier créé, scan terminé, etc.), pour permettre un suivi complet.
Lors de la création d’une demande, un QR code est généré contenant une URL. En scannant ce QR code avec un téléphone, l’utilisateur est redirigé vers l’application qui affiche directement le suivi de ses demandes via l’API.
L’application peut être accessible en localhost ou via l’adresse IP du PC pour un accès mobile.

en gros :
1. Spring Boot (backoffice) → expose les endpoints API
2. Vue.js  → consomme ces endpoints + reçoit les redirections QR
3. framework(frontoffice )→ on change juste l'URL du QR code pour pointer vers Vue.js

TL : Elia - implementation du generation code qr dans FrontOffice(framework) 

Jemima — BACKEND (Ilay backOffice) (Spring Boot):
  branche: sprint 4/feature/expoEndPoint
1. Créer l'entité Demande avec les champs :
    - id, numDemande, numPasseport, typeDemande, statut, dateCreation, historique

2. Créer l'entité HistoriqueStatut :
    - id, statut, dateChangement, demande (FK)

3. Mettre en place le repository JPA pour Demande et HistoriqueStatut

4. Implémenter le service de recherche :
    - Par numéro de demande → retourne cette demande en premier + les autres du même passeport
    - Par numéro de passeport → retourne toutes les demandes par ordre chronologique,du plus recent 

5. Créer l'endpoint REST : GET /api/demandes?numDemande={num}
    - Retourne la demande ciblée en tête + les autres en dessous

6. Créer l'endpoint REST : GET /api/demandes?numPasseport={num}
    - Retourne toutes les demandes triées par date (la plus récente en premier)

7. Générer un QR code à la création de chaque demande :
    - Le QR code contient l'URL du frontend avec le numéro de demande en paramètre
    - Ex : http://<ip-pc>:<port>/suivi?numDemande=12345
    - Utiliser une lib Java (ex: ZXing)

8. Exposer l'endpoint de génération QR code : GET /api/qrcode?numDemande={num}
    - Retourne l'image QR code (PNG ou base64)

9. Configurer CORS pour autoriser les appels depuis le frontend (localhost ou IP PC)

10. Tester tous les endpoints avec Postman ou Swagger


Mioty — FRONTEND (Vue.js)
  branche: sprint 4/feature/utilisationEndPoint/historiqueStatutDemande
1. Initialiser le projet frontend (choisir : Vue.js)

2. Créer la page principale de recherche :
    - Champ saisie : Numéro de demande OU Numéro de passeport
    - Bouton "Rechercher"

3. Lire les paramètres d'URL au chargement de la page :
    - Si ?numDemande=xxx ou ?numPasseport=xxx est présent → lancer la recherche automatiquement
    - (Utilisé quand on scanne le QR code avec un téléphone)

4. Appeler l'API Spring Boot selon la saisie ou le paramètre URL :
    - GET /api/demandes?numDemande={num}
    - GET /api/demandes?numPasseport={num}

5. Afficher les résultats de demandes :
    - La demande principale en haut (mise en avant)
    - Les autres demandes du même passeport en dessous, par ordre chronologique

6. Pour chaque demande, afficher :
    - Numéro de demande
    - Type de demande (ex: Duplicata sans données intérieur)
    - Statut actuel (ex: Approuvé, Dossier créé, Scan terminé...)
    - Historique des statuts avec les dates

7. Afficher l'historique des statuts sous forme de timeline :
    - Ex : Demande créée → Scan terminé →.....

8. Gérer les cas d'erreur :
    - Numéro introuvable → message clair à l'utilisateur
    - Erreur réseau → message d'erreur

9. Rendre l'interface responsive (compatible mobile, utilisé après scan QR)

10. Configurer l'URL de l'API pour fonctionner en localhost ET par IP PC
    - Utiliser une variable d'environnement (ex: VITE_API_URL ou .env)

11. Tester le flux complet :
    - Simuler un scan QR → redirection URL → affichage automatique des demandes

---------------------------------------------------------------------------------
# Sprint 5 : Capture Biométrique et Finalisation Scan

## Contexte

Ajout de captures biométriques (photo d'identité webcam + signature numérique) comme pièces justificatives obligatoires pour finaliser un dossier. Intégration dans visaFO (interface agent).

**Stack existant visaFO :**

* Backend : `ScanController.java`, `ScanService.java`, repository pattern
* Frontend : `scanDemande.jsp`, JS utilitaires dans `FrameWork/src/main/webapp/js/`
* Structure : Spring Boot (ou servlet classique) + JSP

---

## Flux Métier Global

```txt
Agent ouvre dossier (scanDemande.jsp)
  → Upload pièces justificatives (Sprint 3 existant)
  → Capture photo identité (webcam, bloc à ajouter)
  → Capture signature (canvas, bloc à ajouter)
  → Vérification : TOUTES pièces cochées = SCAN_TERMINE
  → Affichage fiche complète (page JSP nouvelle)
```

**Point clé :** `photo` et `signature` sont des **pièces justificatives spéciales** dans `piece_fournie`, pas des tables séparées.

---

## Architecture Clarifiée

### Photo d'Identité + Signature = Pièces Justificatives

**Utiliser table existante `piece_fournie` :**

* Quand agent capture webcam → INSERT dans `piece_fournie` avec `piece_ref_id=99` (ou ID photo d'identité)
* Quand agent capture signature → INSERT dans `piece_fournie` avec `piece_ref_id=100` (ou ID signature)
* `cochee` se met à TRUE **automatiquement** lors de l'upload (pas de table séparée)

### SCAN_TERMINE = Condition Simple

Un dossier/demande est "SCAN_TERMINE" quand **TOUTES les pièces obligatoires pour son `type_titre` sont uploadées** (existent dans `piece_fournie` avec `cochee=TRUE`).

---

## Upload Photo d'Identité (Webcam) - Elia - visaFO `sprint5/feat/photo`

**Fichiers à modifier :**

* Backend Service : `FrameWork/src/main/java/services/ScanService.java` (adapter existant)
* Backend Controller : `FrameWork/src/main/java/controllers/ScanController.java` (adapter existant)
* Frontend JSP : `FrameWork/src/main/webapp/scanDemande.jsp` (ajouter bloc photo)
* Frontend JS : `FrameWork/src/main/webapp/js/photoWebcam.js` (créer nouveau)

### Backend - Model (ORM)

* [ ] Vérifier entity `PieceFournie.java` (dans `FrameWork/src/main/java/models/`)

  * [ ] Vérifier propriétés : `id`, `demande`, `pieceRef`, `cheminFichier`, `nomFichier`, `tailleBytes`, `mimeType`, `uploadedAt`, `cochee`
  * [ ] Si manquantes, les ajouter avec getters/setters

### Backend - Service

* [ ] Dans `ScanService.java` (existant dans `FrameWork/src/main/java/services/`)

  * [ ] Ajouter méthode : `PieceFournie sauvegarderPhotoIdentite(MultipartFile file, Long demandeId, Long dossierId)`

    * [ ] Valider : extension JPG/PNG seulement
    * [ ] Valider : taille ≤ 5MB
    * [ ] Créer répertoire `/uploads/pieces/photos/{dossierId}/` si n'existe pas
    * [ ] Générer nom unique : `{dossierId}_photo_{timestamp}_{hash}.{ext}`
    * [ ] Sauvegarder fichier sur disque
    * [ ] **Créer objet `PieceFournie`** : `demande_id=demandeId`, `piece_ref_id=99 (Photo identité)`, `chemin_fichier`, `nom_fichier`, `taille_bytes`, `mime_type`, `uploaded_at=NOW`, `cochee=TRUE`
    * [ ] Sauvegarder dans BD via repository
    * [ ] Retourner l'objet `PieceFournie` créé
    * [ ] Vérifier automatiquement le statut du scan après upload photo
    * [ ] Rafraîchir le résumé des pièces si toutes les pièces sont présentes

### Backend - Controller

* [ ] Dans `ScanController.java` (existant dans `FrameWork/src/main/java/controllers/`)

  * [ ] Ajouter endpoint : `POST /api/demandes/{demandeId}/dossiers/{dossierId}/photo-identite`

    * [ ] Paramètres : `@PathVariable Long demandeId`, `@PathVariable Long dossierId`, `@RequestParam MultipartFile file`
    * [ ] Appeler `ScanService.sauvegarderPhotoIdentite(file, demandeId, dossierId)`
    * [ ] Retourner JSON : `{ id, nomFichier, uploadedAt, success: true }`
    * [ ] Gérer erreurs : 400 (format/taille invalide), 404 (demande/dossier absent), 500 (erreur serveur)

### Frontend - visaFO (JSP)

* [ ] Dans `scanDemande.jsp` (existant dans `FrameWork/src/main/webapp/`)

  * [ ] Ajouter **bloc "Capture Photo d'Identité"** (après bloc pièces justificatives existant)

### Frontend - visaFO (JS)

* [ ] Créer nouveau fichier : `FrameWork/src/main/webapp/js/photoWebcam.js`

  * [ ] Capturer photo webcam
  * [ ] Prévisualiser photo capturée
  * [ ] Upload photo vers API
  * [ ] Afficher statut upload réussi/échoué
  * [ ] Rafraîchir automatiquement le résumé du scan après upload

---

## Upload Signature (Canvas) - Mioty - visaFO `sprint5/feat/signature`

**Fichiers à modifier :**

* Backend Service : `FrameWork/src/main/java/services/ScanService.java` (adapter existant)
* Backend Controller : `FrameWork/src/main/java/controllers/ScanController.java` (adapter existant)
* Frontend JSP : `FrameWork/src/main/webapp/scanDemande.jsp` (ajouter bloc signature)
* Frontend JS : `FrameWork/src/main/webapp/js/signatureCanvas.js` (créer nouveau)

### Backend - Service

* [ ] Dans `ScanService.java` (existant)

  * [ ] Ajouter méthode : `PieceFournie sauvegarderSignatureCanvas(String base64DataUrl, Long demandeId, Long dossierId)`

    * [ ] Vérifier format base64 : `data:image/png;base64,...`
    * [ ] Décoder base64 → PNG bytes
    * [ ] Valider taille ≤ 1MB
    * [ ] Créer répertoire `/uploads/pieces/signatures/{dossierId}/` si n'existe pas
    * [ ] Générer nom unique : `{dossierId}_sig_{timestamp}_{hash}.png`
    * [ ] Sauvegarder fichier sur disque
    * [ ] **Créer objet `PieceFournie`** : `demande_id=demandeId`, `piece_ref_id=100 (Signature)`, `chemin_fichier`, `nom_fichier`, `taille_bytes`, `mime_type='image/png'`, `uploaded_at=NOW`, `cochee=TRUE`
    * [ ] Sauvegarder dans BD via repository
    * [ ] Retourner l'objet `PieceFournie` créé
    * [ ] Vérifier automatiquement le statut du scan après upload signature
    * [ ] Rafraîchir le résumé des pièces si toutes les pièces sont présentes

### Backend - Controller

* [ ] Dans `ScanController.java` (existant)

  * [ ] Ajouter endpoint : `POST /api/demandes/{demandeId}/dossiers/{dossierId}/signature`

    * [ ] Paramètres : `@PathVariable Long demandeId`, `@PathVariable Long dossierId`, `@RequestBody Map<String, String> { dataUrl }`
    * [ ] Appeler `ScanService.sauvegarderSignatureCanvas(dataUrl, demandeId, dossierId)`
    * [ ] Retourner JSON : `{ id, nomFichier, uploadedAt, success: true }`
    * [ ] Gérer erreurs : 400 (base64 invalide, taille dépassée), 404 (demande/dossier absent)

### Frontend - visaFO (JSP)

* [ ] Dans `scanDemande.jsp` (existant)

  * [ ] Ajouter **bloc "Capture Signature"** (après bloc photo)

### Frontend - visaFO (JS)

* [ ] Créer nouveau fichier : `FrameWork/src/main/webapp/js/signatureCanvas.js`

  * [ ] Capturer signature canvas
  * [ ] Prévisualiser signature
  * [ ] Upload signature vers API
  * [ ] Afficher statut upload réussi/échoué
  * [ ] Rafraîchir automatiquement le résumé du scan après upload

---

## Vérification du Scan et Finalisation - Jemima - visaFO `sprint5/feat/validation`

**Fichiers à modifier :**

* Backend Service : `FrameWork/src/main/java/services/ScanService.java` (adapter existant)
* Backend Controller : `FrameWork/src/main/java/controllers/ScanController.java` (adapter existant)
* Frontend JSP : `FrameWork/src/main/webapp/scanDemande.jsp` (ajouter bloc récapitulatif)
* Frontend JS : `FrameWork/src/main/webapp/js/scanSummary.js` (créer nouveau)

### Backend - Model (ORM)

* [ ] Vérifier entity `Dossier.java` (dans `FrameWork/src/main/java/models/`)

  * [ ] Vérifier propriétés : `scanTermine` (boolean), `dateScanComplete` (LocalDateTime)
  * [ ] Si manquantes, les ajouter avec getters/setters

### Backend - Service

* [ ] Dans `ScanService.java` (existant)

  * [ ] Ajouter méthode : `boolean verifierScanComplet(Long demandeId)`

    * [ ] Récupérer la demande et son type de titre
    * [ ] Récupérer la liste des pièces obligatoires pour ce type de titre
    * [ ] Vérifier que photo d'identité (piece_ref_id=99) existe et cochee=TRUE
    * [ ] Vérifier que signature (piece_ref_id=100) existe et cochee=TRUE
    * [ ] Vérifier toutes les autres pièces obligatoires existent et cochee=TRUE
    * [ ] Retourner true si toutes présentes, false sinon

  * [ ] Ajouter méthode : `Dossier marquerScanTermine(Long dossierId, Long demandeId)`

    * [ ] Appeler `verifierScanComplet(demandeId)`
    * [ ] Si retour false, lever exception `ScanIncompleteException`
    * [ ] Charger le dossier via repository
    * [ ] Mettre à jour : `scanTermine=true`, `dateScanComplete=NOW()`
    * [ ] Sauvegarder le dossier en BD
    * [ ] Mettre à jour le statut de la demande à `SCAN_TERMINE`
    * [ ] Créer entrée dans historique des statuts (`type_changement`, `ancien_statut`, `nouveau_statut`, `date_changement`)
    * [ ] Retourner le dossier mis à jour

### Backend - Controller

* [ ] Dans `ScanController.java` (existant)

  * [ ] Ajouter endpoint : `GET /api/demandes/{demandeId}/scan-status`

    * [ ] Paramètres : `@PathVariable Long demandeId`
    * [ ] Appeler `ScanService.verifierScanComplet(demandeId)`
    * [ ] Récupérer liste complète des pièces fournies pour cette demande
    * [ ] Retourner JSON : `{ scanComplet: boolean, pieces: [ { id, nom, statut, cochee } ], photoId, signatureId }`
    * [ ] Gérer erreurs : 404 (demande absent)

  * [ ] Ajouter endpoint : `POST /api/dossiers/{dossierId}/demandes/{demandeId}/finaliser-scan`

    * [ ] Paramètres : `@PathVariable Long dossierId`, `@PathVariable Long demandeId`
    * [ ] Appeler `ScanService.marquerScanTermine(dossierId, demandeId)`
    * [ ] Retourner JSON : `{ success: true, dossier: {...}, message: "Scan finalisé" }`
    * [ ] Gérer erreurs : 400 (scan incomplet), 404 (dossier/demande absent), 500 (erreur métier)

### Frontend - visaFO (JSP)

* [ ] Dans `scanDemande.jsp` (existant)

  * [ ] Ajouter **bloc "Résumé du Scan et Finalisation"** (après blocs photo et signature) :

    * [ ] Afficher statut du scan (complet/incomplet)
    * [ ] Lister toutes les pièces avec indicateur cochée/manquante
    * [ ] Afficher état photo d'identité (✓/✗)
    * [ ] Afficher état signature (✓/✗)
    * [ ] Bouton "Finaliser le Scan" (actif seulement si scan complet)
    * [ ] Spinner de chargement et message de statut

### Frontend - visaFO (JS)

* [ ] Créer nouveau fichier : `FrameWork/src/main/webapp/js/scanSummary.js`

  * [ ] Fonction `chargerStatutScan(demandeId)` : appel GET `/api/demandes/{demandeId}/scan-status`, met à jour affichage
  * [ ] Fonction `afficherPieceStatus(pieces)` : boucle et affiche chaque pièce avec statut (cochée/manquante)
  * [ ] Fonction `finaliserScan(dossierId, demandeId)` : appel POST `/api/dossiers/{dossierId}/demandes/{demandeId}/finaliser-scan`
  * [ ] Fonction `redirigerVersFiche(dossierId)` : redirige vers `/fiche-demande?dossierId={dossierId}` après finalisation
  * [ ] Gestion erreurs : afficher message utilisateur, retry possible
